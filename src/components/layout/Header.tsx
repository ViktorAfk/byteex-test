import React from 'react';
import { HeaderContainer } from '../common/Container/HeaderContainer';

export const Header = () => {
  return (
    <header className={'bg-iconBackground'}>
      <HeaderContainer />
    </header>
  );
};
